spec code { ver = 5.3 | syn = utinax | sty = full }


load afile
afile.afunc(afile.avar)

load afolder
afolder.afile.afunc(afolder.afile.avar)

load afolder.afile
afile.afunc(afile.avar)


prime afile
afunc(avar)

prime afolder
afile.afunc(afile.avar)

prime afolder.afile
afunc(avar)


prime astruct in afile
create astruct()
