spec {ver = 5.6} | create struct 'single' { on single.create(ctx, info = none) { if info { set single.'info' = info | set single.'full' = true || else | create single.dict('info') | set single.'full' = false } } } | create single('sup')
