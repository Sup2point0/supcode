<sc> <res> <spec[ver = 5.4]> create struct 'single' { on single.create(ctx, info = none) { if info { set single.'info' = info | set single.'full' = true || else | set single.'info' = dict() | set single.'full' = false } } } | create single('sup') </spec> </res> </sc>
