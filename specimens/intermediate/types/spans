spec code { ver = 5.5 | syn = utinax | sty = mixed }


set span('r') = { from 1 to 10 }

loop for i from 2 to 20 by 2 {
  sys.out(i ^ 2)
}


set span('s') = 1~100

loop(0~100~4) {
  sys.out(sqrt(loop.count))
}
