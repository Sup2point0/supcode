\ factorial
\ supcode specimen

spec code { ver = 5.5 | syn = utinax | sty = stan }


create func {
  define factorial(int('input')) {
    if input = 1 {
      func.out(1)
    }

    func.out(input * factorial(input - 1))
  }
}