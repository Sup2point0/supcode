spec code { ver = 5.4 | syn = utinax | sty = stan }


set 'text' = "suppety sup"

sys.out(text#1)
sys.out(text#9~11)
sys.out(case(text)[upper])

sys.out(split(text))
sys.out(split(text)["p"])

set text = "sup", "pety", "sup"

sys.out(join(text))
sys.out(join(text)["-"])
