spec code { ver = 5.5 | syn = utinax | sty = full }

enable sys


sys.out(sys.loc.folder.name)
sys.log(sys.loc.path.len)

sys.open(sys.file('test.exe'))
create sys.file('test.txt') in sys.path('/tests')

if sys.operator('+') {
  evoke sys.error('afflux')
}


sys.exit()
