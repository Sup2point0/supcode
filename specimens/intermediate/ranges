spec code { ver = 5.4 | syn = utinax | sty = mixed }


set list('r') = { from 1 to 10 }

loop for i from 2 to 20 by 2 {
  sys.out(i ^ 2)
}


set list('s') = 1~100

loop for i in 0~100~4 {
  sys.out(sqrt(i))
}
