spec code { ver = 5.4 | syn = utinax | sty = stan }


sys.out("sup")

prompt.open()
prompt.out("sup")

define 'test'(value) {
  func.out(value * 2)
}

set 'out' = {
  loop(20) {
    if loop.count > 2 {
      loop.out(i)
      loop.exit()
    }
  }
}
