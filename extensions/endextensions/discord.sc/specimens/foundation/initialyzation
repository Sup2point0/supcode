spec code { ver = 5.3 | syn = utinax | sty = mixed }


activate discord


create discord.client(name) [
  | key = "token"
  | intents = all
]

name.start()


create discord.bot 'name' [ key = "token" | intents = all ] {
  let bot.commands.prefix = "."
}

name.start()
