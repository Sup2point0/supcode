spec code { ver = 5.5 | syn = utinax | sty = mixed }


create struct 'test' {
  define test.'function'(option) {
    ...
  }
}


create struct('test') {
  create func [struct] {
    define self.function(option) {
      ...
    }
  }
}
