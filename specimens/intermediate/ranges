spec code { ver = 5.3 | syn = utinax | sty = full }


loop for 'idx' from 1 to 10 {
  sys.out(i ^ 2)
}

set list('a') = { from 2 to 20 by 2 }


loop for i in 1~100 {
  sys.out(sqrt(i))
}

set list('s') = 0~100~5
